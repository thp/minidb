[nosetests]
with-doctest = true
cover-erase = true
with-coverage = true
cover-package = minidb

[pep8]
max-line-length = 120
# We use "!= None" for operator-overloaded SQL, cannot use "is not None"
ignore = E711
